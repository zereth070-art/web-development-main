    const cajasTexto = ["Nombre", "email", "Contraseña"];
            cajasTexto.map( 

                (elemento, posicion, arrayentero) => {
             `<input type="text" id="${elemento}" />`;
            return elemento;
        }
        )
        
console.log() 